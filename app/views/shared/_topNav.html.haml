%nav.navbar.navbar-default
  .container-fluid
    .navbar-header
      %button.navbar-toggle.collapsed{"data-target": "#bs-example-navbar-collapse-1", "data-toggle": "collapse", type: "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      =link_to (image_tag "logo.png", class: "logo-image"), root_path, class: "logo-anchor"
    / Collect the nav links, forms, and other content for toggling
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li
          =link_to t(:attempts), attempts_path
      %ul.nav.navbar-nav.navbar-right
        %li.dropdown
          %a.dropdown-toggle{"aria-expanded": "false", "data-toggle": "dropdown", href: "#", role: "button"}
            = "#{t(:language)}: #{t(I18n.locale)}"
            %span.caret
          %ul.dropdown-menu{role: "menu"}
            %li=link_to t(:ka), url_for( locale: 'ka' ), class: "small button"
            %li=link_to t(:en), url_for( locale: 'en' ), class: "small button"
        %li.dropdown
          %a.dropdown-toggle{"aria-expanded": "false", "data-toggle": "dropdown", href: "#", role: "button"}
            =current_student.name
            %span.caret
          %ul.dropdown-menu{role: "menu"}
            %li
              =link_to t(:logout), destroy_student_session_path, method: :delete
    / /.navbar-collapse
  / /.container-fluid
