%nav.navbar{role: "navigation"}
  .container
    .navbar-header
      %button.navbar-toggle.collapsed{type: "button", data: {toggle: "collapse", target: "#navbar"}, aria: {expanded: "false", controls: "navbar"} }
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: "#"}=image_tag "logo.gif", id: "logo"
    #navbar.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li{class: active_class(professors_root_path)}
          %a{href: "#"} Student Home
        %li{class: active_class(professors_root_path)}
          %a{href: "#about"} About
        %li{class: active_class(professors_root_path)}
          %a{href: "#contact"} Contact
    .pull-right
      - if current_student
        Signed in: #{current_student.email}#{link_to "Logout", destroy_student_session_path, method: :delete}
