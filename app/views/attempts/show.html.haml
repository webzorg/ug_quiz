.page-header
  = link_to attempts_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    =t(:back)
  = link_to edit_attempt_path(@attempt), class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-pencil
    =t(:edit)
  %h1=t(:show_attempt)

%dl.dl-horizontal
